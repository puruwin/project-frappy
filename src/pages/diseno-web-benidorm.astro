---
import LocalPageLayout from "../layouts/LocalPageLayout.astro";
import { localPages } from "../data/local-pages";
const page = localPages.find((page) => page.slug === "diseno-web-benidorm")!;
---

<LocalPageLayout {page} />
